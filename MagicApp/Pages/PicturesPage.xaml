<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MagicApp.Pages.PicturesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MagicApp.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Enabled"
    Loaded="Page_Loaded">

    <Grid ColumnDefinitions="Auto,*,Auto" RowDefinitions="*,50">
        <Border
            Grid.Column="0"
            Grid.Row="0"
            Grid.ColumnSpan="3"
            Margin="10"
            CornerRadius="10"
            Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1">
            <Image Name="Picture" Stretch="Fill"/>
        </Border>
        <StackPanel
            Grid.Column="0"
            Grid.Row="0"
            Grid.ColumnSpan="3"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <ProgressRing Name="ProgressRing"/>
        </StackPanel>

        <StackPanel
            Grid.Row="1"
            Grid.Column="0"
            Orientation="Horizontal"
            Margin="10,0,0,0">
            <ComboBox
                Name="ComboBox"
                VerticalAlignment="Center"
                SelectionChanged="ComboBox_SelectionChanged">
                <StackPanel Name="Bing" Orientation="Horizontal" Spacing="10">
                    <Image Height="20" Width="20" Name="BingIcon"/>
                    <TextBlock x:Uid="Pages_Pictures_Source_Bing"/>
                </StackPanel>
                <StackPanel Name="NASA" Orientation="Horizontal" Spacing="10">
                    <Image Height="20" Width="20" Name="NASAIcon"/>
                    <TextBlock x:Uid="Pages_Pictures_Source_NASA"/>
                </StackPanel>
            </ComboBox>
        </StackPanel>
        <StackPanel
            Grid.Row="1"
            Grid.Column="2"
            Orientation="Horizontal"
            Margin="0,0,10,0"
            Spacing="10">
            <Button Name="InfoButton" x:Uid="Pages_Pictures_Button_Info" VerticalAlignment="Center" Width="50">
                <SymbolIcon Symbol="Important"/>
                <Button.Flyout>
                    <Flyout>
                        <StackPanel>
                            <TextBlock Name="InfoTitle" Style="{ThemeResource BaseTextBlockStyle}" FontSize="18" TextWrapping="Wrap" Width="400"/>
                            <TextBlock Name="InfoBody" FontSize="12" TextWrapping="Wrap" Width="400"/>
                        </StackPanel>
                    </Flyout>
                </Button.Flyout>
            </Button>
            <Button Name="DownloadButton" x:Uid="Pages_Pictures_Button_Download" VerticalAlignment="Center" Width="50" Click="DownloadButton_Click">
                <SymbolIcon Symbol="Download"/>
            </Button>
        </StackPanel>
    </Grid>
</Page>