<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MagicApp.Pages.SearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MagicApp.Pages"
    xmlns:controls="using:MagicApp.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Enabled"
    Loaded="Page_Loaded">

    <Grid ColumnDefinitions="Auto,*,Auto" RowDefinitions="50,*">
        <ComboBox
                Name="ComboBox"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                Grid.Column="0"
                Grid.Row="0"
                Margin="25,0,10,0"
                SelectionChanged="ComboBox_SelectionChanged">
            <StackPanel Name="Google" Orientation="Horizontal" Spacing="10">
                <Image Height="20" Width="20" Name="GoogleIcon"/>
                <TextBlock x:Uid="Pages_Search_Engine_Google"/>
            </StackPanel>
            <StackPanel Name="Bing" Orientation="Horizontal" Spacing="10">
                <Image Height="20" Width="20" Name="BingIcon"/>
                <TextBlock x:Uid="Pages_Search_Engine_Bing"/>
            </StackPanel>
            <StackPanel Name="Baidu" Orientation="Horizontal" Spacing="10">
                <Image Height="20" Width="20" Name="BaiduIcon"/>
                <TextBlock x:Uid="Pages_Search_Engine_Baidu"/>
            </StackPanel>
        </ComboBox>
        <AutoSuggestBox Name="SearchBox" x:Uid="Pages_Search_SearchBox" MinWidth="200" Height="35" Grid.Row="0" Grid.Column="1" QueryIcon="Find" TextChanged="SearchBox_TextChanged"/>
        <controls:ActionButton x:Name="OpenInBowser"
                x:Uid="Pages_Search_OpenInBowser"
                Grid.Row="0"
                Grid.Column="2"
                Margin="10,0,25,0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                IconGlyph="&#xE909;"/>

        <Border 
            Grid.Row="1"
            Grid.ColumnSpan="3"
            Margin="10"
            CornerRadius="8"
            Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1">
            <WebView2 Name="explorer"/>
        </Border>
    </Grid>
</Page>
