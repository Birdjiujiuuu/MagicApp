<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MagicApp.Pages.NoticePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MagicApp.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Enabled"
    Loading="Page_Loading">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 预览卡片 -->
        <Border
            Grid.Column="0"
            Margin="10,20,5,20"
            CornerRadius="10"
            Height="200"
            Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Margin="20,20,20,10" Orientation="Horizontal">
                    <Image Height="20" Width="20" Margin="0,0,5,0" VerticalAlignment="Center" Source="ms-appx:///Assets/MagicAppLogo.png"/>
                    <TextBlock Name="AppName" Margin="5,0,0,0" FontSize="14" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Margin="20,10,20,20" Orientation="Vertical">
                    <TextBlock Name="ContentTitlePreview" FontSize="15" TextWrapping="Wrap"/>
                    <TextBlock Name="ContentContentPreview" FontSize="15" TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 设置项 -->
        <StackPanel Grid.Column="1" Margin="5,20,10,20">
            <Grid>
                <Grid.ChildrenTransitions>
                    <TransitionCollection>
                        <EntranceThemeTransition IsStaggeringEnabled="True" FromVerticalOffset="500"/>
                    </TransitionCollection>
                </Grid.ChildrenTransitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <AutoSuggestBox Name="ContentTitle" x:Uid="Notice_ContentTitle" Grid.Row="0" Margin="0,0,0,10" TextChanged="ContentTitle_TextChanged"/>
                <AutoSuggestBox Name="ContentContent" x:Uid="Notice_ContentContent" Grid.Row="1" Margin="0,0,0,10" TextChanged="ContentContent_TextChanged"/>
                <ComboBox Name="SoundEvent_ComboBox" x:Uid="Notice_SoundEvent_ComboBox" Grid.Row="2" Margin="0,0,0,10" SelectedIndex="0">
                    <ComboBoxItem Name="Default" x:Uid="Notice_SoundEvent_Default"/>
                    <ComboBoxItem Name="IM" x:Uid="Notice_SoundEvent_IM"/>
                    <ComboBoxItem Name="Reminder" x:Uid="Notice_SoundEvent_Reminder"/>
                    <ComboBoxItem Name="SMS" x:Uid="Notice_SoundEvent_SMS"/>
                    <ComboBoxItem Name="Alarm" x:Uid="Notice_SoundEvent_Alarm"/>
                    <ComboBoxItem Name="Call" x:Uid="Notice_SoundEvent_Call"/>
                </ComboBox>
                <StackPanel Grid.Row="100" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Name="Clear" Margin="0,0,5,0" x:Uid="Notice_Clear" Click="Clear_Click"/>
                    <Button Name="Send" Margin="5,0,0,0" x:Uid="Notice_Send" Style="{StaticResource AccentButtonStyle}" Click="Send_Click"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Grid>
</Page>
