<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MagicApp.Pages.NoticePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MagicApp.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Enabled"
    Loading="Page_Loading">

    <Grid ColumnDefinitions="*,*">
        <!-- 预览卡片 -->
        <Border
            Grid.Column="0"
            Margin="10,20,5,20"
            CornerRadius="8"
            Height="200"
            Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1">
            <Grid RowDefinitions="Auto,*">
                <StackPanel Grid.Row="0" Margin="20,20,20,10" Spacing="10" Orientation="Horizontal">
                    <Image Height="20" Width="20" VerticalAlignment="Center" Source="ms-appx:///Assets/MagicAppLogo.png"/>
                    <TextBlock Name="AppName" FontSize="14" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Margin="20,10,20,20" Orientation="Vertical">
                    <TextBlock Name="ContentTitlePreview" FontSize="15" TextWrapping="Wrap"/>
                    <TextBlock Name="ContentContentPreview" FontSize="15" TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 设置项 -->
        <StackPanel Grid.Column="1" Margin="5,20,10,20" Spacing="10" Orientation="Vertical">

            <!--动画-->
            <StackPanel.ChildrenTransitions>
                <TransitionCollection>
                    <EntranceThemeTransition IsStaggeringEnabled="True" FromVerticalOffset="500"/>
                </TransitionCollection>
            </StackPanel.ChildrenTransitions>

            <AutoSuggestBox Name="ContentTitle" x:Uid="Pages_Notice_ContentTitle" TextChanged="ContentTitle_TextChanged"/>
            <AutoSuggestBox Name="ContentContent" x:Uid="Pages_Notice_ContentContent" TextChanged="ContentContent_TextChanged"/>
            <ComboBox Name="SoundEvent_ComboBox" x:Uid="Pages_Notice_SoundEvent_ComboBox" SelectedIndex="0">
                <ComboBoxItem Name="Default" x:Uid="Pages_Notice_SoundEvent_Default"/>
                <ComboBoxItem Name="IM" x:Uid="Pages_Notice_SoundEvent_IM"/>
                <ComboBoxItem Name="Reminder" x:Uid="Pages_Notice_SoundEvent_Reminder"/>
                <ComboBoxItem Name="SMS" x:Uid="Pages_Notice_SoundEvent_SMS"/>
                <ComboBoxItem Name="Alarm" x:Uid="Pages_Notice_SoundEvent_Alarm"/>
                <ComboBoxItem Name="Call" x:Uid="Pages_Notice_SoundEvent_Call"/>
            </ComboBox>
            <StackPanel Spacing="10" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Name="Clear" Height="35" x:Uid="Pages_Notice_Clear" Click="Clear_Click"/>
                <Button Name="Send" Height="35" Style="{StaticResource AccentButtonStyle}" Click="Send_Click">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock x:Uid="Pages_Notice_Send"/>
                        <FontIcon Glyph="&#xE724;"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>