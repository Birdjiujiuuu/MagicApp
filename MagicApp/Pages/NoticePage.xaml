<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MagicApp.Pages.NoticePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MagicApp.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Enabled">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="1" Grid.Column="1">
            <Grid>
                <Grid.ChildrenTransitions>
                    <TransitionCollection>
                        <EntranceThemeTransition IsStaggeringEnabled="True" FromVerticalOffset="500"/>
                    </TransitionCollection>
                </Grid.ChildrenTransitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" FontSize="20">
                    <Span>
                        <Bold>在此处编辑您的通知内容</Bold>
                    </Span>
                </TextBlock>
                <AutoSuggestBox Name="ContectTitle" Grid.Row="2" PlaceholderText="标题" Header="标题"/>
                <AutoSuggestBox Name="Contect" Grid.Row="4" PlaceholderText="内容" Header="内容"/>
                <ComboBox Name="SoundEvent_ComboBox" Grid.Row="6" Header="声音事件" SelectedIndex="0">
                    <ComboBoxItem Name="Default" Content="默认"/>
                    <ComboBoxItem Name="IM" Content="IM"/>
                    <ComboBoxItem Name="Reminder" Content="提醒"/>
                    <ComboBoxItem Name="SMS" Content="消息"/>
                    <ComboBoxItem Name="Alarm" Content="闹钟"/>
                    <ComboBoxItem Name="Call" Content="呼叫"/>
                </ComboBox>
                <Button Name="Send" Grid.Row="100" Content="发送" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Right" Click="Send_Click"/>
            </Grid>
        </StackPanel>
    </Grid>
</Page>
