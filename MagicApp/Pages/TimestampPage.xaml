<Page x:Class="MagicApp.Pages.TimestampPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:MagicApp.Pages"
      xmlns:controls="using:MagicApp.Controls"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Loaded="Page_Loaded">

    <Page.Resources>
        <!-- 卡片样式 -->
        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefault}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
        </Style>
    </Page.Resources>
    
    <ScrollViewer>
        <StackPanel Margin="10,20" Spacing="10">
            <!-- 实时显示卡片 -->
            <Border Style="{StaticResource CardBorderStyle}">
                <Grid ColumnDefinitions="*,*">
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Spacing="10">
                        <TextBlock Text="当前时间" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <TextBlock x:Name="CurrentTimeText" TextWrapping="Wrap"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Spacing="10">
                        <TextBlock Text="当前时间戳" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <TextBlock x:Name="CurrentTimestampText" TextWrapping="Wrap"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 日期时间转时间戳卡片 -->
            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel Spacing="10">
                    <TextBlock Text="日期时间转换为时间戳" FontSize="20"/>
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <DatePicker x:Name="DatePickerInput"/>
                        <TimePicker x:Name="TimePickerInput" ClockIdentifier="24HourClock"/>
                        <ComboBox x:Name="SecondsComboBox" Width="70"/>
                        <Button Content="转换" Style="{StaticResource AccentButtonStyle}" Click="OnConvertDateTimeToTimestamp"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="转换结果" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <TextBlock x:Name="ConvertedTimestampResult" VerticalAlignment="Center" Width="150" TextWrapping="Wrap"/>
                        <controls:ActionButton x:Name="CopyTimestampResultButton" IconGlyph="&#xE8C8;" ToolTipService.ToolTip="复制"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- 时间戳转日期时间卡片 -->
            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel Spacing="10">
                    <TextBlock Text="时间戳转换为日期时间" FontSize="20"/>
                    <AutoSuggestBox x:Name="TimestampInput" PlaceholderText="输入秒级时间戳" TextChanged="TimestampInput_TextChanged"/>
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="转换结果" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <TextBlock x:Name="ConvertedDateTimeResult" VerticalAlignment="Center" Width="150" TextWrapping="Wrap"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>